今天发现了一个这样的代码，见下图：



这种情况在API数据提供是本应该统一协商好以何种值返回，比如类似的空值统一用String类型的返回，值可以为空，也可以为"null"，这样前端写代码只需要这样写就可以了，并且心不会虚。



这里补充一下，第一张图中的同事写这么多判断可见不管是从经验上还是从做事严谨上都是点赞的，这里给个建议可能这样写会更好一点。抽出一个公共的方法放在公共的库中，见下图：



这里的好处相信大家都知道，可以重复利用，精简代码，提高可读性和维护性，比如后期发现还有个"false”值要判断，那么直接可以在isEmpty方法里的comparable值补充一个数组值进去就可以，这样系统所有的地方都补充了这个判断。见下图：